server:
  port: ${SERVER_PORT:9003}
spring:
  application:
    name: gateway-admin
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:39.108.136.61}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:39.108.136.61}:${REGISTER_PORT:8848}
        file-extension: yml
#    sentinel:
#      transport:
#        dashboard: ${SENTINEL_DASHBOARD_HOST:39.108.136.61}:${SENTINEL_DASHBOARD_PORT:8021}
