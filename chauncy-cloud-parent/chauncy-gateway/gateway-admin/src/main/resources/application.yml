spring:
  rabbitmq:
    host: ${RABBIT_MQ_HOST:39.108.136.61}
    port: ${RABBIT_MQ_PORT:1884}
    username: ${RABBIT_MQ_USERNAME:admin}
    password: ${RABBIT_MQ_PASSWORD:admins}
  redis:
    host: ${REDIS_HOST:39.108.136.61}
    port: ${REDIS_PORT:6386}
    password: ${REDIS_PASSWORD:w+.8?2267}
    lettuce:
      pool:
        max-active: 300
    database: 1

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:${DATASOURCE_DBTYPE:mysql}://${DATASOURCE_HOST:39.108.136.61}:${DATASOURCE_PORT:8386}/sc_gateway?characterEncoding=UTF-8&useUnicode=true&useSSL=false
    username: ${DATASOURCE_USERNAME:root}
    password: ${DATASOURCE_PASSWORD:%dt6$#@%s}

logging:
  level:
    java:
      sql:
        PreparedStatement: debug
    com.chauncy.cloud: debug
  path: logs/
  file:
    max-size: 1GB

mybatis-plus:
  type-aliases-package: com.chauncy.cloud.*.domain.po.*
  mapper-locations: classpath*:/mapper/**/*Mapper.xml
  typeEnumsPackage: com.chauncy.cloud.*.enums